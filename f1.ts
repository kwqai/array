/*

    PROGRAM #1
    ==========

    function functioname() {
    let a: number=33*2
    return a 
    }
    console.log(functioname());



    /*

    function fi(): number {
        let a: number = 12;
        let b: number = 2;
        let c: number = 3;
    
        let additionResult: number = a + b;
        let subtractionResult: number = a - b;
        let multiplicationResult: number = a * c;
        let divisionResult: number = a / c;
    
        let finalResult: number = additionResult + subtractionResult + multiplicationResult + divisionResult;   
        return finalResult;
    }
    
    console.log(fi());
    
    */

    //  Arrow FUNCTION:

    //  Syntax            let arrowfunction = () => {

    //                     }

88  /*

    PROGRAM #2    
    ==========

    let Arrowfunction = () => {         //  Arrow function will be written with in variable.
        let name: string= "Roshan"
        return name                     //  name functiond will be called here.
        }
        console.log(Arrowfunction());   //  Arrowfunction will be called here.
    
    */

        //  PROBLEM MORE COMPLICATED:
        //  =========================


    let Arrowfunction = () => {         //  Arrow function will be written with in variable.
        let name: string= "Reshamro"
        return name                     //  name functiond will be called here.
        }
        console.log(Arrowfunction());   //  Arrowfunction will be called here.
    
